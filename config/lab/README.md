# Configuraciones pre-armadas para gl-epti

Estos archivos se copian en `/opt/lab-config/` dentro del contenedor.
Son configuraciones base que pueden usarse como referencia o aplicarse
manualmente con `gitlab-ctl reconfigure`.
