# ============================================
# Include: deploy.yml
# Copiar a .gitlab/ci/deploy.yml
# ============================================
deploy-staging:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Desplegando a STAGING..."
    - echo "Build desplegado en ${ENVIRONMENT}"
  environment:
    name: staging
    url: https://staging.${CI_PROJECT_NAME}.example.com
  rules:
    - if: $CI_COMMIT_BRANCH == "develop"
